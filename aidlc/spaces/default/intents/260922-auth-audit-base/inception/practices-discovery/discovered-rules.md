# Discovered Rules — auth-audit-foundation（主担当の初稿・DRAFT）

> **この文書は初稿（DRAFT）です。** ここに載せるのは、人間が明言した「必ず守る／絶対にしない」
> 制約だけです。新規プロジェクトのため、リポジトリの観察からは人間の明言した制約を導けません。
> 下の「面談で確認する候補」は面談で人間が明言した場合にのみ、`ALWAYS ...` / `NEVER ...` の
> 形で本節に移します。明言されなかった候補は削除します。
>
> `aidlc/spaces/default/memory/project.md` に既にある制約（コミット前の承認、相対パス表記、
> `reference/` の扱い、Apache License 2.0 ヘッダーの挿入）は重複させません。

## Mandated

None.

## Forbidden

None.

## 面談で確認する候補（未確定・人間の明言があるまで採用しない）

- 候補M1: ALWAYS 統合先ブランチへの統合前に、lint・フォーマット検査・ビルド・全テストが通っていることを確認する
- 候補M2: ALWAYS 認証・認可・監査ログに関わる変更には、失敗系（拒否・ロック・無効トークン等）のテストを含める
- 候補M3: ALWAYS 本番環境への配備は依頼者の手動承認を得てから行う
- 候補F1: NEVER パスワード・トークン・秘密鍵などの秘密情報をソースコードや設定ファイルにハードコードしたり、ログや監査ログに出力したりしない
- 候補F2: NEVER 統合先ブランチ（`develop` または `main`）へ、テストが失敗した状態のコミットを統合しない
- 候補F3: NEVER `vendor/make-you-chic-ui`（サブモジュール）の中身をこのリポジトリから直接変更しない（変更が必要な場合は make-you-chic-ui のリポジトリ側で行う）

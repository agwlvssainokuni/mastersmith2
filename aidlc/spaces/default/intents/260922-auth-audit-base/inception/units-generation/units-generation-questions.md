# Units Generation — Questions

Domain Design で決めた部品（`aidlc/spaces/default/intents/260922-auth-audit-base/inception/domain-design/components.md` の8部品と、部品ではない共通の仕組み）を、Construction で1つずつ作り上げる「開発の単位（Unit）」にまとめます。ここでは単位の分け方と単位どうしの依存関係だけを決め、どの単位から作るか（順番）は後の Delivery Planning で決めます。

前提（決定済み）:
- 配備の形は、フロントエンドのビルド結果を同梱した実行可能 WAR 1つである（チームの進め方）。単位を分けても、配備の単位は1つのままである。
- アプリの骨格がまだ無いため、最初に作る単位には骨格の一式（起動・内部DB・ヘルスチェック・構造化ログ・フロントエンドのビルドとログイン画面の枠・1コマンドの検査・カバレッジ・CI など）を含める（チームの進め方）。

### Q1. 単位はどの切り口で分けますか？

- A. 機能ごとに、バックエンドと画面をまとめた縦の単位にする（例: 骨格／ログイン・トークン・ログアウト・ロック／管理画面のアクセス制御／監査ログ）。1つの単位が API と画面の両方を持つ
- B. 機能ごとに分けたうえで、バックエンドと画面を別の単位にする（例: 認証のバックエンド／認証の画面側 …）
- C. 部品ごとに1単位にする（Domain Design の8部品＋骨格）
- X. Other (please specify)

[Answer]: A. 機能ごとに、バックエンドと画面をまとめた縦の単位にする（例: 骨格／ログイン・トークン・ログアウト・ロック／管理画面のアクセス制御／監査ログ）。1つの単位が API と画面の両方を持つ

### Q2. 単位の粒度はどの程度にしますか？

- A. 粗め（3〜5単位程度）。単位どうしのつなぎ目が少なく、1単位の作業量は大きい
- B. 細かめ（6〜9単位程度）。1単位の作業量は小さいが、つなぎ目と設計の成果物が増える
- X. Other (please specify)

[Answer]: A. 粗め（3〜5単位程度）。単位どうしのつなぎ目が少なく、1単位の作業量は大きい

### Q3. アプリの骨格と共通の仕組み（起動・内部DB・ヘルスチェック・構造化ログ・分散トレース・共通のエラー応答・ビルドとWARへの同梱・1コマンドの検査・CI）は、どの単位に置きますか？

- A. 独立した「骨格」の単位にする。ほかのすべての単位がこれに依存する
- B. 独立した単位にせず、最初に作る機能の単位に含める
- X. Other (please specify)

[Answer]: A. 独立した「骨格」の単位にする。ほかのすべての単位がこれに依存する

### Q4. 互いに依存しない単位どうしは、並行して作ってよい形にしますか？

依存関係の図の形だけを決めます（実際に並行して作るか、どの順で作るかは Delivery Planning で決めます）。

- A. 並行してよい。依存関係は本当に必要なものだけにする（例: 監査ログは出来事の受け取りだけなので、認証の完成を待たずに作れる形にする）
- B. 一列に並べる。すべての単位が直前の単位に依存する形にする
- X. Other (please specify)

[Answer]: B. 一列に並べる。すべての単位が直前の単位に依存する形にする

### Q5. 単位どうしのつなぎ目（画面とバックエンドの間の API、バックエンド内の出来事）の約束事は、どう扱いますか？

- A. 独立した単位にせず、各単位の設計（Contract Design）で、その単位が提供する API・出来事として決める
- B. API・出来事の約束事だけをまとめた単位（kind: spec）を別に作り、ほかの単位はこれに依存する
- X. Other (please specify)

[Answer]: A. 独立した単位にせず、各単位の設計（Contract Design）で、その単位が提供する API・出来事として決める

## Consolidated Summary Confirmation

- 切り口: 機能ごとに、バックエンドと画面をまとめた縦の単位にする
- 粒度: 粗め（3〜5単位程度）
- 骨格と共通の仕組み: 独立した「骨格」の単位にし、ほかのすべての単位がこれに依存する
- 依存関係の形: 一列に並べ、すべての単位が直前の単位に依存する
- 単位どうしの約束事（API・出来事）: 独立した単位にせず、各単位の設計（Contract Design）で決める
- 配備: 単位を分けても、配備は実行可能 WAR 1つのまま（決定済み）

Does this all look correct before I generate the artifact?

- Looks correct
- Request changes

[Answer]: Looks correct

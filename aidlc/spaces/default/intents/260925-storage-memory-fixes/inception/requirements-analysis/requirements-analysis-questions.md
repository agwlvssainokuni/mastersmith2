# Requirements Analysis の質問（260925-storage-memory-fixes）

依頼の4件のうち、直す向きが分かれる点だけを質問にしました。根拠は `aidlc/spaces/default/codekb/mastersmith2/code-quality-assessment.md` の TD-1〜TD-4 です。

## 決まっていること（質問にしない）

- 4件目（`perf/dsl-timing.sh` 33 行の説明の「要件の条件は 1g」）は、前の Intent の決定（2g）に合わせて説明の文だけを直す。既定の値（55 行）と `perf/README.md` はすでに 2g。
- この Intent には Performance Validation の段が無いため、直した後の負荷の試験（k6 の `dslMixed`・`dslCycle` と `perf/dsl-timing.sh`）の持ち主は Build and Test とする（`project.md` の決まり）。直す前の値は前の Intent の実測（`dslMixed` で `anon` 1,894MiB＝2g の 93%、`memory.events` の `max` 2,340 回。10MB の投入と適用で1回あたり約 10.8MB 伸び、40 回で約 483MB）を使う。
- DSL を信頼できない入力として扱う決まり（大きさ・深さ・別名の上限、タグの拒否、重複キーの誤り、誤りの行と列）は弱めない。本文の識別（SHA-256）とダウンロードはバイト単位で一致させる。
- 不具合を直すときは、その不具合を再現するテストを同じコミットに入れる（`project.md` の決まり）。

## Question 1
内部DB のファイルの伸び（1件目）について、直した後に満たすべき状態はどれですか？
今は、10MB の DSL の投入と適用で1回あたり約 10.8MB 伸び続け、止めるまで縮みません。保存する本文の論理的な最大は、プレビュー1件と履歴 20 件で約 210MB と見積もられています。

A. 動いている間もファイルの大きさが頭打ちになる（例: 10MB の DSL の投入と適用を 40 回続けても、論理的な最大の約 210MB に余裕を足した値を超えない。数値は設計で決める）
B. 頭打ちまでは求めず、1回あたりの伸びを今の半分以下に抑える
C. 伸びは受け入れ、アプリを止めずにファイルを詰め直す手段（管理者の操作など）を用意する
X. Other (please specify)

[Answer]: A（依頼者の補足: H2の使用によりConnectionが生きている間はコンパクションが実行されない。このため内部DBのファイルの伸びを抑制するにはH2のConnectionを全てcloseするタイミング・方法を検討する必要がある。） **Mode:** guided

## Question 2
1件目の直し方で、表の形を変える場合の「1つ前の版に戻せること」の扱いはどれにしますか？
`team.md` の決まりでは、DB の変更は前進のみで、1つ前の版のアプリが動く後方互換を保ちます。今の版は本文を `yaml_bytes` の列から読むため、列を消したり中身の形（圧縮など）を変えたりすると、前の版のイメージへ戻せなくなります。

A. 1つ前の版に戻せることを守る（前の版が読む列と中身の形は残す。そのために新しい形との二重の書き込みなどが要るなら受け入れる）
B. 表の形は変えず、H2 の設定や保存の仕方の工夫だけで直す（直せる範囲が狭くなりうる）
C. この直しに限り、前の版のイメージへの戻しは諦め、戻すときは配備の前に取った内部DB のバックアップから戻す
X. Other (please specify)

[Answer]: B **Mode:** guided

## Question 3
10MB の DSL の投入とログインを重ねたときのメモリ（2件目）について、直した後に満たすべき状態はどれですか？
今は、前の Intent と同じ `dslMixed` の条件で、プロセスのメモリ（`anon`）がコンテナの上限 2g の 93% に達し、上限に当たった回収（`memory.events` の `max`）が 2,340 回起きています。内訳（ヒープ・ヒープ以外・ページキャッシュ）はまだ測っていません。

A. 先に内訳を測り、アプリで減らせる分（本文の複写・読み込みの途中の形など）を減らしたうえで、同じ `dslMixed` の条件で `anon` が 2g の 80% 以下、OOMKilled なしを満たす
B. 先に内訳を測り、その結果を見てから数値の目標を依頼者が決める（コード生成の計画の承認の場で決める）
C. `memory.events` の `max` を 0 回にする（上限に当たらない）
X. Other (please specify)

[Answer]: B **Mode:** guided

## Question 4
`AccessTokenApiIT` の一時的な失敗（3件目）は、どう扱いますか？
6 件がすべて 0.587 秒のうちに `Connection reset` で落ち、同じ時刻に Gradle の作業プロセスとの接続も時間切れでした。原因の候補は (a) PC の負荷、(b) colima のポートの転送とテストのアプリの空いた番号の衝突、(c) 1つの JVM に多数の Spring の文脈がたまることによる資源の使い過ぎ、で、どれも確かめていません。1回目の実行の報告は上書きされていて、後から確かめる材料はほぼ残っていません。`team.md` では、不安定なテストは原因を直すまで統合しません。

A. 再現と切り分けを試し（(b)・(c) を狙った試し）、原因が確かめられたらその直しを入れる。再現できなければ、候補に対する予防の変更（例: テストのアプリの待ち受けを 127.0.0.1 に限る）と、次に失敗したときに材料が残る仕組み（テストの報告の保存など）を入れ、原因は未確定として記録する
B. 再現と切り分けだけを行い、原因が確かめられたときだけ直す（再現できなければ調べた結果の記録だけにする）
C. 再現は試さず、候補に対する予防の変更と、材料が残る仕組みだけを入れる
X. Other (please specify)

[Answer]: B **Mode:** guided

## Follow-up Questions

## Question F1
Q1 の補足（H2 は接続が1本でも生きている間は詰め直しをせず、すべての接続を閉じる必要がある）を受けて、動いている間にすべての接続を閉じて詰め直すのは、いつにしますか？
今は、接続のプール（Hikari、上限 30）が接続を持ち続けるため、DB が閉じるのはアプリの停止時だけです。閉じている間は、ログイン・監査の記録・DSL の操作など、内部DB を使うすべての要求が DB を使えません。

A. DSL の適用（と投入）の直後に、そのつど閉じて詰め直す（伸びる操作の直後だけ行う）
B. ファイルの大きさが決めた値を超えたときに、次の空いた時点で閉じて詰め直す（しきい値は設計で決める）
C. 管理者が画面や API から操作したときだけ閉じて詰め直す
D. いつにするかは、閉じ方と詰め直しにかかる時間を確かめてから、コード生成の計画の承認の場で決める
X. Other (please specify)

[Answer]: C **Mode:** guided

## Question F2
すべての接続を閉じて詰め直す間に届いた、内部DB を使う要求（ログイン・トークンの更新・監査の記録・DSL の操作など）はどう扱いますか？
10MB の本文が 21 件ある状態の詰め直しにかかる時間は、まだ測っていません。

A. 詰め直しが終わるまで待たせる（接続を借りる待ちの上限 5 秒を超えたら、今と同じ失敗の応答になる）
B. 詰め直しの間は、内部DB を使う要求をすぐに断る（決まった `code` の Problem Details で「しばらくしてから」と返す）
C. 詰め直しの間の要求は失われてもよい扱いにはせず、監査の記録だけは取りこぼさないことを必須とする（待たせ方・断り方は設計で決める）
X. Other (please specify)

[Answer]: A **Mode:** guided

## Question F3
Q4 で「再現と切り分けだけを行い、確かめられたときだけ直す」を選びました。一方 `team.md` には「不安定なテストは放置せず、原因を直すまで統合しない」とあります。再現できなかったとき、この Intent の統合はどうしますか？

A. 再現できなければ、`AccessTokenApiIT` は不安定なテストと確かめられていない扱いとし、調べた結果を記録して統合してよい
B. 再現できなければ、再現の試しを続けるか、Q4 の答えを変えるか（予防の変更を入れる）を、その時点で依頼者が決める
X. Other (please specify)

[Answer]: A **Mode:** guided

## Question F4
Q1 の目標（A: 動いている間も頭打ち。10MB の投入と適用を 40 回続けても約 210MB＋余裕を超えない）と、F1 の答え（C: 管理者が操作したときだけ詰め直す）を合わせると、管理者が操作しない限りファイルは今と同じように伸び続けます。直した後に満たすべき状態はどちらですか？

A. 管理者が詰め直しを行った直後に、アプリを止めずに、ファイルが論理的な最大（約 210MB）に余裕を足した値以下に戻ること（操作しない間の伸びは今のまま受け入れ、README の既知の制約に書く）
B. 管理者の操作に加えて、決めた大きさを超えたら自動で詰め直す（F1 に B を足す）
X. Other (please specify)

[Answer]: A **Mode:** guided

## Question F5
管理者の詰め直しの操作は、どこから行えるようにしますか？
今の管理者向けの画面には、DSL の管理の画面があります。管理者向けの API は `/api/admin/**` で、サーバー側で管理者だけに限っています。

A. 管理者向けの API と画面の両方（画面にボタンと、実行前の確認と、処理中の表示を置く）
B. 管理者向けの API だけ（画面は作らず、README に呼び方を書く）
X. Other (please specify)

[Answer]: B **Mode:** guided

## Question F6
詰め直しの操作を監査ログに残しますか？
今は、認証・アクセス拒否・DSL の操作を監査ログに残しています。F2 の答え（A: 終わるまで待たせる）では、詰め直しが接続を借りる待ちの上限（5 秒）より長くかかると、その間のログインや監査の記録が失敗しうることになります。

A. 残す（誰が・いつ・結果、詰め直しの前と後のファイルの大きさ）。詰め直しの最中の監査の記録の失敗は、今の監査の決まり（書き込みに失敗したときの扱い）に従う
B. 残さない（アプリのログにだけ出す）
X. Other (please specify)

[Answer]: A **Mode:** guided

## Consolidated Summary Confirmation

- 1件目（内部DB のファイルの伸び）: H2 は接続が生きている間は詰め直さないため、すべての接続を閉じて詰め直す仕組みを作る。行うのは管理者が操作したときだけ（F1: C）。入口は管理者向けの API だけで、画面は作らず README に呼び方を書く（F5: B）。目標は、詰め直しの直後に、アプリを止めずに、ファイルが論理的な最大（約 210MB）に余裕を足した値以下に戻ること。操作しない間の伸びは受け入れ、README の既知の制約に書く（Q1: A、F4: A）。
- 表の形は変えず、H2 の設定や保存の仕方の工夫だけで直す。1つ前の版に戻せることはそのまま保たれる（Q2: B）。
- 詰め直しの間に届いた内部DB を使う要求は、終わるまで待たせる。接続を借りる待ちの上限（5 秒）を超えたら今と同じ失敗の応答になる（F2: A）。
- 詰め直しの操作は監査ログに残す（誰が・いつ・結果・前と後のファイルの大きさ）。最中の監査の記録の失敗は、今の監査の決まりに従う（F6: A）。
- 2件目（メモリ）: 先に内訳（ヒープ・ヒープ以外・ページキャッシュ）を測り、数値の目標はコード生成の計画の承認の場で依頼者が決める（Q3: B）。
- 3件目（`AccessTokenApiIT`）: 再現と切り分けだけを行い、原因が確かめられたときだけ直す（Q4: B）。再現できなければ、不安定なテストと確かめられていない扱いとし、調べた結果を記録して統合してよい（F3: A）。
- 4件目: `perf/dsl-timing.sh` 33 行の説明の文だけを 2g に合わせる（決まっていること）。
- 直した後の負荷の試験（k6 の `dslMixed`・`dslCycle` と `perf/dsl-timing.sh`）は Build and Test で行い、前の Intent の実測と比べる（決まっていること）。

Does this all look correct before I generate the requirements artifact?

- Looks correct
- Request changes

[Answer]: Looks correct

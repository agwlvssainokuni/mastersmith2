# Incident Response — 質問

前提（読み取りだけで調べた結果。2026-09-25）:
- 障害の対応の記録（runbooks・incident-plan・escalation-matrix）は、これまでの Intent に無い。この段で初めて作る。範囲は、今動いているアプリ全体（ログイン・アクセス制御・監査ログ・DSL の管理・対象DB の接続）とする。
- 配備先は開発者の PC 上のコンテナ（アプリ・見本の対象DB・必要なときだけ手元の監視）。利用者と運用者は依頼者1名で、AI が調べと手順の実行を補助する（team.md の Way of Working）。知らせの先（通知）は配備先が決まるまで作らない（project.md）。警報は Grafana の画面で見るだけ。
- 既にある手順: 戻し方（`operation/deployment-pipeline/rollback-runbook.md`、README の「戻し方」）、内部DB のバックアップと戻し方（README）、監査ログの確かめ方（README）、内部DB のファイルの大きさの見方（README の「DSL の管理」）、警報 16 件（`docker/monitoring/provisioning/alerting/mastersmith.yaml`）。
- 起こりうる主な障害（この Intent で増えたものを含む）: アプリが止まる・healthy にならない／メモリの上限（2g）に近い（10MB の DSL を重ねたときに約 92%）／内部DB のファイルが増え続ける（U4-STORAGE-RUN）／見本の対象DB が止まる・応答しない（生成と照合が「接続できない」、照合が最悪 23〜28 秒で他の重い操作が `DSL_BUSY`）／起動のときに適用中の DSL を読めない（ERROR を1件出して適用中が無い状態で起動）／監査の書き込みの失敗・接続プールの待ち／ログインの失敗の多発・ロック／配備の後の不具合（戻す。古い版の起動は未確認 U4-MIGRATION）。

## Q1. 受け手と判断

A. 依頼者1名が受け手で判断者。AI は、依頼者に頼まれたときに調べ（ログ・状態・監査の複写の読み取り）と手順の実行を補助し、止める・戻す・データを戻すなどの操作は依頼者の承認を得てから行う。知らせは Grafana の画面で見るだけ（配備先が決まったら通知の先を決める）
B. ほかの人も受け手に加える（名前と連絡の手段を決める）
X. Other (please specify)

[Answer]: A

## Q2. 復旧の目標と、内部DB のバックアップ

内部DB（利用者・監査ログ・DSL の適用の履歴）はコンテナのボリュームにあり、今は配備の前などに手で複写している。

A. 復旧の目標は「1日の内に戻す（RTO 1日）・失ってよいのは最後の手での複写の後の変更まで（RPO は最後の複写まで）」とし、複写は今のまま（配備の前・大きな DSL の操作の前・監査の確かめのときに手で行う）
B. 復旧の目標は「数時間の内（RTO 数時間）・失ってよいのは1日分まで（RPO 1日）」とし、毎日1回の複写の手順（PC の定期実行の仕組みで、アプリを止めて複写する）を README に足す。定期実行の登録は依頼者が行う
X. Other (please specify)

[Answer]: A

## Q3. 障害の重さの段階

A. 2段にする。「高」: アプリが止まる・ログインできない・データが壊れた・秘密情報が漏れた疑い（すぐ対応、必要なら戻す）。「低」: 一部の機能が使えない・遅い・警告（当日か翌日に対応）
B. 4段（SEV1〜SEV4。一般的な区分）にする
X. Other (please specify)

[Answer]: A

## Consolidated Summary Confirmation

回答をまとめると、この段では次のとおり決める。

1. 受け手と判断者は依頼者1名。AI は依頼者に頼まれたときに調べ（ログ・状態・監査の複写の読み取り）と手順の実行を補助し、止める・戻す・データを戻す操作は依頼者の承認を得てから行う。知らせは Grafana の画面で見るだけ（配備先が決まったら通知の先を決める）（Q1: A）。
2. 復旧の目標は RTO 1日・RPO は最後の手での複写まで。内部DB の複写は今のまま、配備の前・大きな DSL の操作の前・監査の確かめのときに手で行う（Q2: A）。
3. 重さは2段（高: アプリが止まる・ログインできない・データが壊れた・秘密情報が漏れた疑い → すぐ対応し、必要なら戻す。低: 一部の機能が使えない・遅い・警告 → 当日か翌日に対応）（Q3: A）。
4. 成果物は `incident-plan.md`（段階・流れ・記録の仕方・事後の振り返り）、`escalation-matrix.md`（重さごとの受け手と、AI に頼めること）、`runbooks.md`（起こりうる障害ごとの見分け方と手順。既にある手順は README・rollback-runbook を参照し、足りないもの（見本の対象DB が止まった・適用中の DSL を読めない・`DSL_BUSY` が続く・メモリの上限に近い・内部DB のファイルの伸び・警報ごとの初動）を書く）。クラウドの仕組み（SSM・Incident Manager・AWS Backup）は配備先が決まるまで作らない。

Does this all look correct before I generate the artifact?

- Looks correct
- Request changes

[Answer]: Looks correct


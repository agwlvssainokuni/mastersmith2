# Deployment Pipeline — Questions

配備先は、前の Intent と同じく開発者の PC 上のコンテナだけである。配備の仕組みは、既にある `Dockerfile`・`compose.yaml`・README と、前の Intent の配備の手順（`aidlc/spaces/default/intents/260922-auth-audit-base/operation/deployment-pipeline/` の `deployment-strategy.md`・`rollback-runbook.md`・`cd-config.md`）を正として使う。つまり、止めてから新しい版で起動する入れ替え、手での配備、手でのスモークテスト、直前の版のコミットを取り出して作り直す戻し方である。

今回の変更（コミット `d948544`）は、内部DBの接続プールの上限の既定値を 10 から 30 にしただけで、スキーマの変更と依存関係の変更は無い。`compose.yaml` は `.env` の値をすべてアプリに渡すため、新しい環境変数 `MASTERSMITH_DB_MAXIMUM_POOL_SIZE` のために構成のファイルを変える必要は無い。前回配備した版は `7040876` である。

ここでは、今回の変更に固有で、まだ決まっていない点だけを確認する。

### Q1. 配備の後に問題が出たとき、どう戻しますか？

今回の変更は設定の値だけである。そのため、直前の版に戻す前に、環境変数で上限を下げる手が使える。この手なら、作り直しは要らず、`.env` に1行加えて再起動するだけで済む。ただし、上限を 10 に下げると、F2（同時 10 件のログインで監査が欠ける）が戻る。

- A. まず `.env` に `MASTERSMITH_DB_MAXIMUM_POOL_SIZE` を設定して上限を下げ、アプリを再起動する（設定の戻し）。それで直らなければ、直前の版 `7040876` へ戻す（既存の `rollback-runbook.md` の手順）
- B. 既存の手順どおり、直前の版 `7040876` へ戻すだけにする（設定の戻しは使わない）
- X. Other (please specify)

[Answer]: A. まず `.env` に `MASTERSMITH_DB_MAXIMUM_POOL_SIZE` を設定して上限を下げ、アプリを再起動する（設定の戻し）。それで直らなければ、直前の版 `7040876` へ戻す（既存の `rollback-runbook.md` の手順）

### Q2. 要件 FR6.2（k6 で同時 10 件のログインを流し、監査が欠けないことを確かめる）は、配備のどの時点で行いますか？

負荷の試験は、配備した環境とは別の使い捨ての環境（`docker/perf/compose.yaml`）で行う決まりである。`perf/README.md` の手順では、資源を取り合わないように、配備したアプリを止めてから行い、終わったら起動し直す。

- A. 本番の配備の前に行う。使い捨ての環境で k6 を流して確かめ、通ってから、配備したアプリを新しい版に入れ替える（問題があれば配備しない）
- B. 本番の配備とスモークテストの後に行う。配備したアプリを止めて使い捨ての環境で k6 を流し、終わったら配備したアプリを起動し直す（要件の文言どおり）
- X. Other (please specify)

[Answer]: A. 本番の配備の前に行う。使い捨ての環境で k6 を流して確かめ、通ってから、配備したアプリを新しい版に入れ替える（問題があれば配備しない）

### Q3. 戻しの手順の練習を、今回の配備で行いますか？

前の Intent では、戻しの練習を行わず、「次に版を替えるときに確かめる」と記録した（前の Intent の `rollback-runbook.md` 6節と Deployment Execution の記録）。今回が、その「次に版を替えるとき」にあたる。ただし、今回の版と直前の版 `7040876` はスキーマが同じ（V1〜V4）である。そのため、前回残した前提（直前の版が、新しいスキーマの変更を含む DB で起動できること）は、今回の練習では確かめられない。

- A. 行う。新しい版を配備してスモークテストを通した後、直前の版 `7040876` に戻してスモークテストを通し、最後に新しい版へ配備し直す（手順そのものが動くことを確かめる。スキーマの前提は未確認のまま残す）
- B. 行わない。スキーマの変更がある版を配備するときに、改めて練習する
- X. Other (please specify)

[Answer]: B. 行わない。スキーマの変更がある版を配備するときに、改めて練習する

## Follow-up Questions

Q1 で「まず設定で戻す」を選んだ場合に、上限をいくつに下げるかが決まっていない。上限を下げると、同時の数がその値に達したときに F2（監査の欠落）が起きうる。

### FQ1. 設定で戻すとき、上限をいくつに下げますか？

- A. 20（同時 19 件までは監査が欠けない。メモリなどの問題が上限 30 に原因がある場合の中間の値）
- B. 10（元の値。F2 は元に戻る）
- C. 値は決めず、その都度、問題の内容を見て依頼者が決める
- X. Other (please specify)

[Answer]: B. 10（元の値。F2 は元に戻る）

## Consolidated Summary Confirmation

回答をまとめると、次のとおりです。

- 配備の仕組み: 前の Intent の配備の手順（入れ替え、手での配備、手でのスモークテスト、直前の版を取り出して作り直す戻し方）をそのまま使う。構成のファイル（`compose.yaml` など）は変えない
- 戻し方: 問題が出たら、まず `.env` に `MASTERSMITH_DB_MAXIMUM_POOL_SIZE=10` を加えて再起動する（設定の戻し。F2 は元に戻る）。それで直らなければ、直前の版 `7040876` へ戻す（Q1: A、FQ1: B）
- k6 の確かめ（FR6.2）: 本番の配備の前に、使い捨ての環境で同時 10 件のログインを流して確かめる。通ってから本番の配備を行い、問題があれば配備しない。要件の文言（配備した後）とは順序が違うため、その差を成果物に明記する（Q2: A）
- 戻しの練習: 今回は行わない。スキーマの変更がある版を配備するときに改めて練習する。前の Intent から残る未確認の前提は、そのまま残す（Q3: B）

Does this all look correct before I generate the artifact?

- Looks correct
- Request changes

[Answer]: Looks correct
